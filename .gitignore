# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Logs / temp
*.log
node_modules/
.cache/

# Local personal copies (keep private résumé/letters out of the repo)
*_PRIVATE.*
my_*.html
applications.csv

# Local preview / scratch (not part of the product)
PREVIEW_*.html
*.tmp

# Claude Code session context (local only — never push)
CLAUDE.md

# Local-only development assets (the public repo ships only the app itself).
# apply_copilot_app.html is the local source of truth — index.html is generated from it.
apply_copilot_app.html
tests/
supabase/
